import styled from 'styled-components';

import { custom } from '@/styles';

const SignInTitle = styled(custom.AlertDialogTitle)`
  text-align: center;
`;

const SignInDescription = styled(custom.AlertDialogDescription)`
  text-align: center;
`;

export { SignInTitle, SignInDescription };
